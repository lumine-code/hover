{
  "name": "hover",
  "author": "lumine-code",
  "description": "Show documentation tooltips and signature help at the cursor.",
  "repository": "https://github.com/lumine-code/hover",
  "bugs": {
    "url": "https://github.com/lumine-code/hover/issues"
  },
  "keywords": [
    "tooltip",
    "documentation",
    "signature-help",
    "datatip",
    "quick-info",
    "lsp"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "You can read the documentation of the symbol under the cursor with {{ 'hover:toggle' | keystroke }}",
    "You can see the signature of the call you are inside with {{ 'hover:toggle-signature-help' | keystroke }}"
  ],
  "main": "./lib/main",
  "files": [
    "docs",
    "keymaps",
    "lib",
    "spec",
    "styles"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "consumedServices": {
    "hover.provider": {
      "versions": {
        "^1.0.0": "consumeHover"
      }
    },
    "hover.signature-provider": {
      "versions": {
        "^1.0.0": "consumeHoverSignature"
      }
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  },
  "configSchema": {
    "showOnMouseMove": {
      "title": "Show On Mouse Move",
      "description": "Show the hover tooltip when the mouse pointer rests on a symbol.",
      "type": "boolean",
      "default": true
    },
    "showOnCursorMove": {
      "title": "Show On Cursor Move",
      "description": "Show the hover tooltip when the cursor rests on a symbol. When disabled, the tooltip appears only via the `hover:toggle` command.",
      "type": "boolean",
      "default": false
    },
    "showDelay": {
      "title": "Show Delay",
      "description": "How long (in milliseconds) the mouse pointer or the cursor must rest before the hover tooltip is requested.",
      "type": "number",
      "minimum": 0,
      "default": 250
    },
    "hideDelay": {
      "title": "Hide Delay",
      "description": "How long (in milliseconds) the mouse pointer must be away from the hovered symbol before the tooltip is dismissed. Time spent over the tooltip itself does not count.",
      "type": "number",
      "minimum": 0,
      "default": 250
    },
    "showSignatureWhileTyping": {
      "title": "Show Signature Help While Typing",
      "description": "Show the signature help overlay automatically when a trigger character is typed, and keep it updated while typing function arguments.",
      "type": "boolean",
      "default": true
    },
    "includeSignatureDocumentation": {
      "title": "Include Signature Documentation",
      "description": "Include the documentation of the whole signature in the signature help overlay, below the documentation of the active parameter.",
      "type": "boolean",
      "default": false
    }
  }
}
